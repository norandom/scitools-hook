.venv/
__pycache__/
*.pyc
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.und
.coverage
htmlcov/

# The gate writes this when `baseline` is run; this repository does not use adaptive mode,
# and a baseline captured by accident from whatever the tree happened to be is not a
# decision (task 9.3 found two such artefacts at this root, task 10.4 removed the risk).
scitools-hook.baseline.json

# `mkdocs build` output. The published site is built by .github/workflows/docs.yml.
site/
